# Local development uses Caddy at https://impostor.localhost and proxies /api to
# the local Worker.
#
# Leave this unset for same-origin /api calls through the main app hostname.
#
# Optional when the Worker API is deployed on another production origin:
# VITE_API_BASE_URL=https://impostor-api.example.workers.dev
